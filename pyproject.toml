[project]
name = "lens-behavioural-lending"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
  "fastapi==0.115.0",
  "psycopg[binary]==3.2.3",
  "uvicorn[standard]==0.30.6",
]

[tool.vercel]
entrypoint = "backend.app:app"
